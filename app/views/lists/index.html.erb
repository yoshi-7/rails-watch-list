<div class="container">
  <h1>Your Lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY"), width: 400, height: 300 %>
  </ul>
</div>
